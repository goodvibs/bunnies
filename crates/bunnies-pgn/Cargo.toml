[package]
name = "bunnies-pgn"
version = "0.1.0"
edition = "2024"
description = "PGN parser and renderer for the bunnies chess library."
license = "MIT"

[dependencies]
bunnies = { path = "../bunnies" }
indexmap = "2.14.0"
logos = "0.16.1"
regex = "1.12.3"
